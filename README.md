--- Python FastAPI Backend for Personal Movie Collection with PostgreSQL ---

# MovieShelf 🎬

MovieShelf — это учебный проект на Python, в котором реализуется REST API для управления личной библиотекой фильмов. 
Пользователи смогут регистрироваться, авторизоваться, добавлять фильмы из TMDB, оставлять отзывы и управлять своей коллекцией.


---

## 🔹 Функционал

- Регистрация и авторизация пользователей с использованием JWT
- CRUD для фильмов: добавление, просмотр, редактирование, удаление
- Фильтрация и поиск фильмов по названию, жанру, рейтингу
- Асинхронная работа с PostgreSQL через SQLAlchemy
- Миграции базы данных с Alembic
- Валидация данных с помощью Pydantic
- Интеграция с TMDb API для автоматического импорта информации о фильмах

---

## 🔹 Технологии

- FastAPI — фреймворк для backend-разработки
- SQLAlchemy (async) — ORM для работы с PostgreSQL
- Alembic — миграции базы данных
- Pydantic v2 — валидация данных
- PostgreSQL — база данных
- TMDB API — источник данных о фильмах
- JWT Auth — аутентификация пользователей

---

## 🔹 Прогресс разработки

- День 1

Создан .env с базовыми настройками проекта и настроен requirements.txt
Реализованы файлы:
1) config.py — конфигурация проекта и подключение переменных окружения
2) database.py — асинхронное подключение к базе данных через SQLAlchemy

- День 2

Добавлены основные модели (models.py):
1) User
2) Movie 
3) Review

Созданы Pydantic-схемы (schemas.py)  
Подключён Git и создан репозиторий MovieShelf  
Начало ведения документации и структуры проекта


- День 3

Начата работа с маршрутизаторами (routers/auth_router.py).
Реализован endpoint /register для регистрации пользователя:
1) Хеширование пароля с помощью bcrypt 
2) Проверка уникальности email
3) Добавление пользователя в базу данных и возврат данных через Pydantic-схему UserReadSchema

Реализован endpoint /login для аутентификации:
1) Использование OAuth2PasswordRequestForm для обработки формы логина
2) Проверка email и пароля
3) Генерация JWT-токена с полезной нагрузкой (sub и email) и сроком действия 
4) Создана функция create_access_token для генерации JWT

- Day 4–5 

1) Реализована и полностью настроена JWT-авторизация: регистрация, логин, получение текущего пользователя.  
2) Исправлена логика декодирования токена — теперь sub сравнивается с user.id, а не с email.  
3) Настроено корректное использование SECRET_KEY и других параметров из .env.  
4) Исправлена авторизация в Swagger UI — теперь токен корректно принимается и обрабатывается.  
5) Добавлены новые схемы TokenSchema и LoginSchema.  
6) Временно все файлы перенесены в папку src/ для корректных импортов и упрощения структуры проекта.  
7) Проведён рефакторинг auth-модуля, проверены эндпоинты /register, /login, /auth/me.

- Day 6

1) В config.py перенесены TMDB_API_KEY и
    TMDB_ACCESS_API_KEY из main.py
2) Создан репозиторий utils и файл tmdb.py для работы с TMDB API
3) Начата работа с маршрутизатором /movies_router, реализованы CRUD endpoints

- День 7 — Состояние проекта  

На данный момент весь функционал, связанный с routers в проекте MovieShelf, успешно реализован и протестирован.
1) Добавление фильмов  
Работает полностью для кастомных фильмов с ручной указкой title, genre, description и rating.  
Все проверки валидности (например, рейтинг от 0 до 5) реализованы, некорректные данные возвращают корректный HTTP 400, а не 500.
2) Получение фильмов пользователя  
Эндпоинт /movies/my возвращает все фильмы, добавленные текущим пользователем
3) Удаление фильма  
Эндпоинт /movies/delete/{movie_id} корректно удаляет фильм, проверяя принадлежность пользователю и существование фильма
4) Обновление рейтинга  
Эндпоинт /movies/{movie_id}/rate обновляет локальный рейтинг фильма, с проверкой диапазона 0–5  
Все ручки проверены вручную и через тестовые скрипты. На текущий момент проект стабилен в части работы с маршрутами и базой данных

- День 8 — Система отзывов (Reviews)

Сегодня полностью реализован функционал отзывов к фильмам. Все эндпоинты протестированы и работают стабильно.
1) Добавление отзывов  
Реализован эндпоинт /reviews/add, позволяющий пользователям оставлять несколько отзывов к одному фильму.
Проверяется существование фильма и корректность оценки (0–5).
2) Получение отзывов по фильму  
Эндпоинт /reviews/get/{movie_id} возвращает все отзывы выбранного фильма.
Если фильм не существует — корректная ошибка 404.

3) Редактирование и удаление отзывов  
Пользователь может изменять и удалять только свои отзывы.
Эндпоинты:  
- /reviews/edit/{review_id}
- /reviews/delete/{review_id}

4) Автоматическое обновление рейтинга фильма  
После добавления, изменения или удаления отзыва рейтинг фильма автоматически пересчитывается на основе всех оценок.

5) Обработка ошибок  
Исправлены проблемы с сохранением невалидных отзывов, теперь некорректные данные не попадают в БД, а пользователю возвращаются корректные ошибки.